; == GENERAL =============================================================
; Report an agent's remaining life.
to report-life-remaining
  type self print life-remaining
end

; Report if something died.
to report-death
  ;type self print " has died!"
end

; Produce one last tick and end the model.
; Used to wrap things up in the event of all hives collapsing.
to finalise-model
  tick
  stop
end
; == END GENERAL =========================================================
